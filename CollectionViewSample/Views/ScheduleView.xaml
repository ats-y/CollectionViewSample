<?xml version="1.0" encoding="UTF-8"?>

<!-- 通常スケジュール表示用View-->
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="CollectionViewSample.Views.ScheduleView">
    <ContentView.Content>
        <StackLayout>
            <Label
                Text="単一予定"/>
            <Label
                Text="{Binding Time
                        , StringFormat='{0:yyyy/M/d H:mm}'}" />
            <Label
                Text="{Binding Title}" />

        </StackLayout>
    </ContentView.Content>
</ContentView>